name: tekartik_html
version: 0.3.1
description: An html abstraction layer to work with both dart:html and the html package
homepage: https://github.com/tekartik/tekartik_html.dart
publish_to: none

environment:
  sdk: ^3.12.0
dependencies:
  args: ">=2.7.0"
  tekartik_common_utils:
    git:
      url: https://github.com/tekartik/common_utils.dart
    version: '>=0.10.7'
  html: '>=0.15.0'
  web: '>=0.5.0'

dev_dependencies:
  test: ">=1.31.0"
  dev_build: ">=1.1.7+3"
  process_run: ">=1.2.1+1"
  build_runner: ">=2.15.0"
  build_web_compilers: ">=4.4.19"
  build_test: '>=1.3.7'

dependency_overrides:
  web: '>=1.0.0'
